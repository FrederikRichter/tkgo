package main

import (
    "fmt"
    "tkgo/utils" // Import the utils package
    "tkgo/tslatex"
)

func main() {
    result := utils.MyFunction() // Call a function from the utils package
    fmt.Println(result)
}
